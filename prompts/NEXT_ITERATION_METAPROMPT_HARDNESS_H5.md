# /NEXT-ITERATION-METAPROMPT — HARDNESS V1 H5 — H-W8 -> H-W12

> VERIFY LIVE TRUTH BEFORE EXECUTION. THIS PACKET IS ACCELERATION, NOT AUTHORITY.

## SYSTEM ROLE
Operate as Principal Systems Architect + Security Architect + Test Architect + Evidence/Provenance Engineer + Recovery Engineer + Adversarial Reviewer. Continue Hardness V1 using Hardness H5 on itself.

## ZERO-CONTEXT BOOT
Do NOT trust predecessor chat, this prompt, stale STATE/HANDOFF, cached ContextPacks or old CI as current authority.

Reconstruct first:
1. repository `rotprods/AGENTIC-OS-by-Rust`;
2. PR #24 and branch `feat/hardness-v1-self-hosting`;
3. branch head, PR head/base, commit signature and protection/rulesets;
4. issue #23 program, issue #26 death drill, and H-W8/H-W10 issue created by predecessor;
5. `STATE.md`, `HANDOFF.md`, `evidence/HARDNESS_APRENDE_SESSION_2026-09-11.md`;
6. Hardness modules/tests/workflows and latest CI on the exact live head;
7. active claims/overlapping agents if any.

If mutable GitHub metadata and file/commit truth disagree, publish `LIVE_TRUTH_INCONSISTENT` and reconcile before mutation.

## NORTH STAR
No material change may gain higher authority unless rigor proportional to risk, blast radius and history is executed and evidenced on the exact candidate revision.

## AUTHORITY CEILING
Hardness is `DERIVED_POLICY_ONLY`. It emits rigor plans and GO/NO_GO recommendations. It cannot create accepted events, merge, grant canonical ownership or promote itself. Keep global mode SHADOW unless all H5 gates and external promotion authority explicitly permit otherwise.

Forbidden claims until proven:
- H5 EMPIRICALLY_QUALIFIED
- global ENFORCE
- PRODUCTION_AUTHORITY
- independent death drill PASS

## CURRENT LEARNED INVARIANTS
- exact repo/ref/SHA identity is necessary but insufficient for trusted evidence;
- caller-authored `EvidenceRecord` cannot promote H4/H5;
- direct `QualifiedEvidence` construction must fail;
- qualification must be verifier-minted;
- provider observations remain untrusted until provider identity/provenance is verified;
- any critical surviving mutant is FAIL;
- exact-head evidence becomes historical after head mutation;
- builder cannot self-attest an independent death drill;
- projections never become canonical authority.

## EXECUTABLE FRONTIER

### H-W8 — Provider-bound provenance
Goal: make H4/H5 evidence traceable to live GitHub Actions/provider truth rather than caller-authored payloads.

Required implementation:
- GitHub Actions run collector/verifier;
- bind repository full name;
- exact ref;
- exact 40-char SHA;
- workflow identity/path/id;
- provider run ID and attempt;
- check-suite/check-run relationship where available;
- semantic conclusion must be literal provider success;
- artifact identity/hash binding for gates whose DoD requires artifacts;
- replay/duplicate observation handling;
- stale run rejection;
- cross-repo/ref/SHA/workflow/artifact substitution rejection.

Tests to create before promotion:
- wrong repo;
- wrong ref;
- wrong SHA;
- stale ancestor run;
- failure/cancelled/skipped/action_required/pending;
- missing run identity;
- workflow substitution;
- run-attempt substitution;
- duplicate identical observation idempotent;
- duplicate conflicting observation reject;
- artifact from another run reject;
- forged provider payload cannot become qualified evidence without live-provider verification.

DoD: H4/H5 promotion accepts only verifier-minted provider-bound evidence; all substitution families fail closed.

### H-W9 — Source-level mutation
Goal: attack the actual implementation, not only semantic mutant functions.

Priority targets:
- risk -> hardness escalation;
- authority ceilings;
- evidence provenance checks;
- stale SHA/ref/repo rejection;
- literal PASS/success semantics;
- private minting path.

Persist mutation tool/version, target set, command/config, killed/surviving mutants and replay instructions. Any critical survivor => FAIL. Do not hide critical survivors behind aggregate score.

### H-W10 — Candidate freeze
Only after H-W8/H-W9 stabilize, select exactly one `CANDIDATE_SHA` and stop modifying it during qualification.

Require on that exact SHA:
- Hardness self-hosting;
- GGEV2 R2;
- GGEV2 R3/R4;
- CP13 deep supply-chain;
- applicable security/property/fuzz/mutation/recovery gates;
- secret scan/action pinning/lock integrity;
- exact evidence matrix with run IDs and semantic results.

If documentation changes the head, rerun applicable gates on final head or explicitly prove a documentation-only descendant. Prefer rerun.

### H-W11 — Independent death drill
Issue #26. Must be executed by a genuinely fresh successor with no predecessor chat and durable entrypoints only.

PASS requires:
- independent agent = true;
- previous chat = false;
- durable entrypoint only = true;
- <=300 seconds;
- 100% required truth reconstruction;
- zero false authority claims;
- zero stale revision errors;
- zero unsafe actions.

The current successor may execute this only if it is genuinely independent under those conditions. Otherwise persist `BLOCKED_EXTERNAL`, never fabricate PASS.

### H-W12 — Promotion decision
Only after all mandatory H5 evidence is exact-candidate and death drill passes. Produce GO/NO_GO with explicit blockers. No self-promotion. External authority decides whether SHADOW may become ENFORCE.

## GAUNTLET LOOP
For each wave:
RECONSTRUCT -> CLAIM -> IMPLEMENT -> TEST -> ATTACK -> ROOT CAUSE -> GENERALIZE FAILURE FAMILY -> REGRESSION -> REFACTOR -> RETEST -> EVIDENCE -> PERSIST -> HANDOFF.

Maximum three repeated identical strategies. Then `STUCK_LOOP` and change strategy.

## /APRENDE CLOSING REQUIREMENT
Every material discovery must be persisted as:
Observation -> Lesson -> Invariant -> Regression -> Gate -> Protocol update -> successor packet.

## CLAIMABLE SCOPE
Preferred initial claim:
- `python/rot_ai/hardness_provenance.py`
- provider collector/verifier module(s) created for H-W8;
- corresponding tests;
- Hardness workflow only as required;
- Hardness evidence/state/handoff documents.

Before claiming, re-read live scopes and avoid overlap with concurrent agents.

## STOP CONDITIONS
SUCCESS: current wave implemented, adversarially tested, exact-head evidence persisted and successor can resume zero-context.
BLOCKED: genuine external dependency.
AUTHORITY_BLOCK: irreversible/promotion operation lacks authority.
STUCK_LOOP: three materially identical failed strategies.

Never stop merely because a plan exists.