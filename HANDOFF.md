# HANDOFF — HARDNESS V1 H5 SELF-HOSTING

Authority: zero-context recovery projection. **VERIFY LIVE TRUTH BEFORE EXECUTION.** Live GitHub + exact-SHA CI + durable contracts/evidence outrank this file when stale.

## Mission
Continue Hardness as the assurance layer around CGEV2/GGEV2, using Hardness H5 on itself.

North Star: no material change may gain higher authority unless rigor proportional to risk, blast radius and history is executed and evidenced on the exact candidate revision.

## Entry points
- repo: `rotprods/AGENTIC-OS-by-Rust`
- PR: #24
- branch: `feat/hardness-v1-self-hosting`
- program: issue #23
- empirical death drill: issue #26
- successor packet: `prompts/NEXT_ITERATION_METAPROMPT_HARDNESS_H5.md`
- learning record: `evidence/HARDNESS_APRENDE_SESSION_2026-09-11.md`

## Critical live-truth warning
At predecessor close, GitHub branch/PR metadata reported head `9daf88a7b5a688514e93c9c2f0431b245621fd44`, unsigned and unprotected, while branch file reads already reflected later provenance-hardening content. The predecessor also persisted new learning/metaprompt commits after that observation. Do not infer the current head from this file. Re-fetch branch, PR, commit history, target blobs and CI and reconcile before mutation. If disagreement remains, state `LIVE_TRUTH_INCONSISTENT` and fail closed.

## What is implemented
- H0-H5 deterministic risk compiler and monotonic profiles;
- executable SkillSpecs/lifecycle compiler;
- authority ceilings and fail-closed unknown risk;
- CGEV2 -> Hardness shadow adapter;
- GGEV2 Hardness projection with no canonical mutation authority;
- exact-head evidence binding;
- deterministic fuzz campaigns;
- bounded semantic mutation campaigns with critical-survivor fail;
- chaos/recovery contract and death-drill evaluator;
- supply-chain preflight consuming stronger CP13 surfaces;
- `/autoprompting` compiler;
- H4/H5 rejection of caller-authored evidence;
- verifier-minted `QualifiedEvidence` with direct construction forbidden.

## Escaped failures converted to invariants
1. `EVIDENCE_OBJECT_FORGERY`: correct repo/ref/SHA/PASS does not make caller-authored evidence trusted.
2. `QUALIFIED_EVIDENCE_FORGERY`: trusted-looking wrapper cannot be caller-minted; verifier controls minting.
3. `PROVIDER_OBSERVATION_SUBSTITUTION`: OPEN. Provider payload remains untrusted until live provider identity/provenance is saturated.

Read the learning record for root causes and protocol rules.

## Last independently observed exact-head evidence
Hardness V1 Self-Hosting Gate run `34480609787` completed SUCCESS on `9daf88a7b5a688514e93c9c2f0431b245621fd44`. This is historical evidence once the branch head changes; do not use it to qualify a descendant. The run was a PR event on PR #24 and bound to that SHA.

## Current authority ceiling
Hardness remains `DERIVED_POLICY_ONLY` / intended global mode `SHADOW`. Do NOT claim:
- H5 `EMPIRICALLY_QUALIFIED`;
- global `ENFORCE`;
- `PRODUCTION_AUTHORITY`;
- independent death drill PASS.

## Next safe frontier
H-W8 Provider Provenance:
1. reconstruct live truth and claim non-overlapping scope;
2. implement live GitHub Actions provider collector/verifier;
3. bind repo/ref/exact SHA/workflow/run attempt/check suite and artifacts where required;
4. add substitution/replay/stale-run adversarial corpus;
5. keep provider observations evidence-only;
6. exact-head self-hosting gate;
7. persist /aprende and handoff.

Then H-W9 source-level mutation -> H-W10 frozen exact candidate -> H-W11 independent death drill -> H-W12 external promotion decision.

## DoD law
Implementation alone is NOT DONE. Required: tests created + executed + passed, adversarial/security implications reviewed, evidence bound to exact candidate, docs/state/learning/handoff updated, no unresolved P0/P1 introduced, and zero-context continuation possible.